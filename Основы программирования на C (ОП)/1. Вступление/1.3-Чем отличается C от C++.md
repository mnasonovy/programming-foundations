# Чем отличается C от C++

C и C++ — это родственные языки.
C++ появился на базе C, но со временем превратился в самостоятельный и гораздо более мощный инструмент.

Разберёмся коротко и по делу.

---

## 1. Парадигма программирования

### С — процедурный язык

В C программа строится вокруг **функций и данных**.

Вы пишете:

- функции
- структуры (`struct`)
- глобальные и локальные переменные

Логика — сверху вниз, шаг за шагом.

---

### C++ — объектно-ориентированный язык

C++ добавляет:

- классы
- объекты
- наследование
- инкапсуляцию
- полиморфизм

То есть код можно организовать вокруг **объектов**, а не только функций.

---

## 2. Работа с памятью

### В C:

Память выделяется вручную:

```c
malloc()
free()
```

Вы полностью контролируете процесс.
И полностью отвечаете за ошибки 🙂

В C++:
```с++
new
delete
```

А также:
- **конструкторы** — автоматически инициализируют объекты при создании
- **деструкторы** — освобождают ресурсы при уничтожении объекта
- **умные указатели** (`std::unique_ptr`, `std::shared_ptr`) — управляют памятью автоматически, предотвращая утечки


Появляется больше инструментов для безопасной работы с памятью.

## 3. Стандартная библиотека

### В C

Минимальный набор:

- `stdio.h`
- `stdlib.h`
- `string.h`

Всё довольно низкоуровневое.

---

### В C++

Огромная стандартная библиотека (STL):

- `std::vector`
- `std::string`
- `std::map`
- `std::algorithm`

Есть контейнеры, шаблоны и алгоритмы,
позволяющие писать мощные программы без изобретения велосипеда.

---

## 4. Работа с вводом/выводом

### С

```c
printf("Hello");
```


### C++

```cpp
std::cout << "Hello";
```

C++ использует **потоковую модель ввода-вывода**.

---

## 5. Совместимость

C++ почти полностью поддерживает код C,
но современные версии обоих языков уже развиваются **независимо**.

---

## Краткое сравнение

| Характеристика          | C           | C++                      |
|:------------------------|:------------|:-------------------------|
| Тип языка               | Процедурный | Объектно-ориентированный |
| Классы                  | ❌           | ✅                        |
| STL                     | ❌           | ✅                        |
| Шаблоны                 | ❌           | ✅                        |
| Низкоуровневый контроль | ✅           | ✅                        |
| Сложность               | Ниже        | Выше                     |


---

## Когда что использовать?

**С подходит, если:**

- системное программирование
- драйверы
- embedded-разработка
- нужна максимальная простота и контроль

**C++ подходит, если:**

- большие проекты
- игровая разработка
- сложные архитектуры
- нужны контейнеры и ООП

---

## Итог

**С** — это минималистичный, быстрый и максимально близкий к железу инструмент.

**C++** — это расширенный, более мощный язык, который позволяет строить сложные системы.

Оба языка — серьёзные инструменты.
Просто **C — это фундамент**,
а **C++ — надстройка над ним**.


